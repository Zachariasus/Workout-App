# Netlify: always revalidate the app shell + service worker so a new deploy
# reaches the phone immediately instead of being served stale from the CDN.
# (Offline still works — the service worker keeps its own cached copy.)
/index.html
  Cache-Control: no-cache
/sw.js
  Cache-Control: no-cache
/
  Cache-Control: no-cache
